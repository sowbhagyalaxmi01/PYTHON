#Data Types 
   #A data type tells Python what kind of data a variable contains.
#    variables can store different types of data.

# built-in data types:
#    numerical  type
#    sequence type
#    mapping type
#    Boolean type
#    set type
#    none type
#    binary types


# numerical type:
# 1.int(whole numbers)
#   x=20
#   x=-5
# 2.float(decimal numbers)
#   x=2.2
# 3.complex(number with real and imaginary parts)
#   x=1j

# sequence type:
#   1.str(text data)
#   name="sowbhagya"
#   2.list(ordered,changeable,mutable)
#    x=["app","map"]
#    print(type(x))
#    <class 'list'>
#   3.tuple(ordered,immutable)
#    x=("app","map")
#   4.range(sequence of numbers with specifes range)
#    x=range(4)

# mapping type:
#   dict(key-value pairs)
#   x={"name":"jack"}

# boolean type:
#   bool(represent one of two logical values True or False)
#   is_student = True
#   is_raining = False

# set types:
#   set(stores unique values and does not allow duplicate elements.)
#   x={"sat","job"}
#   frozen set(immutable,version of set)
#   x=frozenset({"asap",banana})

# none type:
#   x=None

