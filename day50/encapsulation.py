#encapsulation is wrapping of data and funtion into single unit(object)
#data +funtion called as capsule
# It helps hide internal data from direct outside access.
# Data is usually kept private and accessed through public methods like getters/setters.
# Improves data security and control.
# Makes code easier to maintain and modify.
# Prevents unwanted or invalid changes to data.
# Example: private int balance; can be accessed/changed through methods like getBalance() and setBalance().g